apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{ .Release.Name }}-{{ .Chart.Name }}
  name: {{ .Release.Name }}-{{ .Chart.Name }}
spec:
  type: ClusterIP
  ports:
    - name: http
      targetPort: 4000
      port: 80
  selector:
    app: {{ .Release.Name }}-{{ .Chart.Name }}

